// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:3.6.1'
        // ❌ Removed deprecated Bintray plugin
        // ❌ Removed android-maven-gradle-plugin
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
        // JitPack automatically adds itself during build
    }
}
